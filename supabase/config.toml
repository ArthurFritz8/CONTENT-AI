# Gerado como base mínima; rode `supabase init` para mesclar defaults completos do CLI.
project_id = "content-ai"

[functions.telegram-bot]
verify_jwt = false # Telegram authenticates via secret header + chat/user allowlist (ADR-018).
